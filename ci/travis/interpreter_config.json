{
  "type": "interpreter-config",
  "configurations": [
    {
      "settings": [
        {
          "driverOptions": {
              "host": "localhost",
              "port": 8080
           },
          "browserOptions": {
              "browserName": "firefox",
							"browserVersion": "latest",
							"platformName": "macOS 10.13",
              "username": "${SAUCE_USERNAME}",
              "accessKey": "${SAUCE_ACCESS_KEY}",
              "tunnel-identifier": "${GITHUB_RUN_ID}",
              "build": "${GITHUB_SHA}",
              "tags": "${GITHUB_REF_NAME}"
           }
        }
      ],
      "scripts": [
          "ci/selenium2-tests/*"
      ]
    }
  ]
}
