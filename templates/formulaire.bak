
<{$formulaire.javascript}>
<form name="<{$formulaire.name}>" action="<{$formulaire.action}>" method="<{$formulaire.method}>" <{$formulaire.extra}>>
<{if $issingle != "on"}>
<center>
	<table>
	<tr>
	<td width=25%>&nbsp;</td>
	<td>
		<table cellpadding=3 cellspacing=1 border=1>
		<tr>
		<th valign=middle>
			<center><h2><nobr><b><{$tempaddingentry}></b></nobr></h2></center>
		</th>
		<td class=head valign=middle>
			<center><h2><nobr><a href="<{$tempformurl}>&select=1"><{$tempviewentries}></a></nobr></h2></center>
		</td>
		</tr>
		</table>
	</td>
	<td width=25%>&nbsp;</td>
	</tr>
	</table>
</center>
	<hr>
<{/if}>
	<table cellspacing="1" class="outer"  width=98%>
		<tr><th colspan="2"><{$formulaire.title}></th></tr>
		<{foreach item=element from=$formulaire.elements}>
		<{if $element.hidden != true}>
		<tr>
			<td class="head"><{$element.caption}></td>
			<td class="<{cycle values="even,odd"}>"><{$element.body}></td>
		</tr>
		<{/if}>
		<{/foreach}>
	</table>
	<{foreach item=element from=$formulaire.elements}>
	<{if $element.hidden == true}>
		<{$element.body}>
	<{/if}>
	<{/foreach}>
</form>
