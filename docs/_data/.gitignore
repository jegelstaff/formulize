# Ignore everything in this directory
*
# Except these: .gitignore itself, and nav.yml, which is hand-authored (the
# main site menu) rather than generated at build time like everything else
# here, so it has to be committed to survive a fresh checkout.
!.gitignore
!nav.yml
