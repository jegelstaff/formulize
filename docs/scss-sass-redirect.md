---
permalink: documentation/scss-sass/
redirect_to: /documentation/
redirect_from:
 - developers/scss-sass/
sitemap: false
---

{% comment %}
  Formulize stopped using SCSS and Sass; the page that described the toolchain
  was last accurate several versions ago and said so in its own first line, so
  it has been deleted rather than left to be found. This keeps the address
  alive - it is linked from nothing on the site, but it has been a published
  URL for years - and sends it to the documentation hub.

  The old /developers/scss-sass/ address was served by the deleted page's
  redirect_from, so it is listed here instead. Handled by jekyll-redirect-from,
  the same as every other moved page.
{% endcomment %}
